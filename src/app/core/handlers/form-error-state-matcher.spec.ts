import { FormErrorStateMatcher } from './form-error-state-matcher';

describe('FormErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new FormErrorStateMatcher()).toBeTruthy();
  });
});
